class a {
    void init(id) {
        self.id = id;
    }
}

void function main() {
    obj_list = [];

    # Measure start time
    start_time = clock();

    for (i = 0; i < 10000; i+=1) {
        obj_list.add(new a(i));
    }

    # Measure end time
    end_time = clock();
    print("Time taken to create 10 objects: ", end_time - start_time, "s");
}
